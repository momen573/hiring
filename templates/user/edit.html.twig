{% extends 'admin.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
    <div class="container mx-auto p-6">
        <h1 class="text-3xl font-semibold text-gray-800 mb-6">Create New User</h1>

        <div class="bg-white p-6 rounded-lg shadow-md">
            {{ include('user/_form.html.twig') }}
        </div>

        <div class="mt-4">
            <a href="{{ path('app_user_index') }}" class="text-indigo-600 hover:text-indigo-800">Back to list</a>
        </div>
    </div>
{% endblock %}
