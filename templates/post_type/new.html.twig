{% extends 'admin.html.twig' %}

{% block title %}New PostType{% endblock %}

{% block body %}
    <h1 class="text-3xl font-semibold text-gray-800 mb-6">Create New PostType</h1>

    <div class="mb-6">
        {{ include('post_type/_form.html.twig') }}
    </div>

    <div class="mt-4">
        <a href="{{ path('app_post_type_index') }}" class="text-blue-500 hover:text-blue-700 underline">Back to list</a>
    </div>
{% endblock %}
