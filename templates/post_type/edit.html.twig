{% extends 'admin.html.twig' %}

{% block title %}Edit PostType{% endblock %}

{% block body %}
    <h1 class="text-3xl font-semibold text-gray-800 mb-6">Edit PostType</h1>

    <!-- PostType Edit Form -->
    <div class="mb-6">
        {{ include('post_type/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <!-- Back to the PostType index page -->
    <div class="mt-4">
        <a href="{{ path('app_post_type_index') }}" class="text-blue-500 hover:text-blue-700">Back to list</a>
    </div>


{% endblock %}
