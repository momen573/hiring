{% extends 'admin.html.twig' %}

{% block title %}New Category{% endblock %}

{% block body %}
    <h1 class="text-3xl font-semibold text-gray-800 mb-6">Create New Category</h1>

    <div class="mb-6">
        {{ include('category/_form.html.twig') }}
    </div>

    <div class="mt-4">
        <a href="{{ path('app_category_index') }}" class="text-blue-500 hover:text-blue-700">Back to list</a>
    </div>
{% endblock %}
